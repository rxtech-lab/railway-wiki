PRODUCT_BUNDLE_IDENTIFIER = app.rxlab.railwaywiki
IPHONEOS_DEPLOYMENT_TARGET = 26.0
TARGETED_DEVICE_FAMILY = 1,2

AUTH_ISSUER_URL = https:/$()/auth.rxlab.app
AUTH_REDIRECT_URI = railwaywiki:/$()/oauth-callback
AUTH_SCOPES = openid profile email
OSM_MAP_STYLE_PATH = /maps/styles/basic/style.json
